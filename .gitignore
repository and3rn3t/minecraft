# Minecraft Server Data
data/
backups/
plugins/

# Configuration files with secrets
.env
config/*.conf
config/*.json
config/audit.log
!config/README.md
!config/*.conf.example
!config/nginx-minecraft.conf

# Temporary files
*.log
*.tmp
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db
desktop.ini

# IDE files
# Editor settings: the directory is ignored, but the two files that describe
# how this repo should be linted and tested are shared deliberately.
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea/
*.sublime-project
*.sublime-workspace

# Deploy agent state (scripts/deploy-agent.sh)
.deploy/

# Docker volumes
docker-volumes/

# Build artifacts
*.jar
# Keep server.jar if it is manually placed
!server.jar

# Python (if using pre-commit)
__pycache__/
*.py[cod]
*$py.class
.python-version
venv/
.venv/
env/

# Node.js (if web panel added)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.next/
out/
dist/

# Test coverage and tooling caches (generated)
.coverage
htmlcov/
coverage.json
coverage.xml
coverage-gaps.txt
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Playwright (generated)
playwright-report/
test-results/
blob-report/
playwright/.cache/

# Local development
.local/
*.local

# Local CodeQL runs (make codeql)
.codeql-db/
codeql-results.sarif
