[Unit]
Description=Minecraft Server Automated Backup
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
# Backups copy the whole world directory while the server may still be live;
# keep that I/O and CPU contention from causing tick lag for players.
Nice=10
IOSchedulingClass=idle
User=%i
Group=%i
WorkingDirectory=/home/%i/minecraft-server
ExecStart=/home/%i/minecraft-server/scripts/backup-scheduler.sh
StandardOutput=journal
StandardError=journal

# Environment variables
Environment="PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"

[Install]
WantedBy=multi-user.target
